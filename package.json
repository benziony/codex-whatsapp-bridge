{
  "name": "codex-whatsapp-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.11"
  },
  "scripts": {
    "check": "node scripts/check.mjs",
    "doctor": "node scripts/doctor.mjs",
    "setup": "node scripts/setup.mjs",
    "setup:apply": "node scripts/setup.mjs --apply",
    "status": "node scripts/codex-whatsapp-client.mjs status",
    "test": "node --test && PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s test -p 'test_*.py'",
    "update:check": "node scripts/update-check.mjs"
  },
  "license": "MIT"
}
